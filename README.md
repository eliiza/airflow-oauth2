Add OpenID support to Airflow docker.

Configure using the following env variables:

- AIRFLOW_OPENID_NAME - The name of the OpenID provider
- AIRFLOW_OPENID_DISCOVERY_URL - The URL of the OpenID provider
